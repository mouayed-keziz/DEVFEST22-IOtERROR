import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitortest.apptest',
  appName: 'app-test',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
